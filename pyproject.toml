[project]
name = "somasafe-firmware"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bleak>=3.0.2",
    "ai-edge-litert==2.1.5",
    "matplotlib>=3.10.9",
    "pyqt6==6.11.*",
    "pyserial>=3.5",
    "numpy>=2.4.6",
]

[dependency-groups]
dev = [
  "ipython",
]

[tool.pyrefly]
python-platform = "linux"
project-includes = [
    "scripts/*.py",
]

[tool.pyrefly.errors]
implicit-import = false

